<h1>Search#index</h1>
<p>Your search for "<%= @keywork_terms%>" return <%= pluralize(@searches.size, 'result')%></p>

<ul>
    <% @searches.each do |search|%>
        <li><%= link_to search.name, search%></li>
    <% end%>
</ul>